'use strict';

var car = require('../source/car');

describe('car', function() {

    //All Cars need a horn
    it('should honk when asked', function() {
        expect(car(action)).toEqual('honk honk');
    })

    //Allow for general case


});